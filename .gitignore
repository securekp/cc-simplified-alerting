# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Packaging output. `static/` and `default/` are the Cribl pack layout that
# scripts/prepare-git-pack.mjs materializes for "Import from Git"; release CI force-adds
# them onto the tag, so they are build artifacts everywhere except on a release commit.
/build
/package-build
/static
/default

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
